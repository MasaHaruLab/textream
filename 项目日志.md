# textream fork 项目日志

- 2026-07-10 【里程碑】选型落定：GitHub 调研后弃自建，采用 f/textream v1.6.2（词级语音跟随/录屏隐身/置顶原生全有）。brew 装官方版验收通过（中文跟随 OK）。
- 2026-07-10 【里程碑】首个魔改落地：新增 See-through (no blur) 开关——去掉毛玻璃模糊、只留可调黑色淡罩，录制时能同时看清提词器后面的屏幕内容。无 Xcode，用 swiftc 手工编译 + 官方包作资源骨架 + ad-hoc 重签替换安装。CEO 待 live 验收。
- 2026-07-10 【技术困难·根因已明】中文卡顿复现成功：wordLevelMatch 稿件侧按空格切词（CJK 不感知，SpeechRecognizer.swift:947），中文整篇成一个"词"→词级策略恒返 0 → ①无恢复机制：偏差超过字符级 ±5 字重同步窗口即卡死（模拟：缺口15字,指针停在33,人已读到95）②bestOffset 与 0 平均,小步前进被砍半 → 粘滞感。次因：contextualStrings 同样按空格切（:445）,整句垃圾提示词喂给识别器拉低中文识别率。触发器：同音字/数字串误识、55s 抢占重启（:756）、跳读。复现脚本 scratchpad/repro_stall.swift。修复方案已设计,待 CEO 拍板。
- 2026-07-10 【突破】卡顿修复落地（3 刀）：①charLevelMatch 加 n-gram 重锚点（CJK 3字/拉丁 6字连排锚,前视 40 字符）②bestOffset 单侧为 0 不再求平均 ③contextualStrings 剔除含 CJK 的整句垃圾提示。A/B 回归 7 场景全过：中文缺口15/25字由卡死转恢复,英文 gap 场景同步改善,正常场景零回归。回归工具 scripts/match-repro-ab.swift（swift 直接跑）。已构建替换安装,待 CEO live 验收。
- 2026-07-10 【技术困难→突破】CEO 实测中文仍卡（"走着走着不动了"）。二次根因：①start(with:) 会把稿子重建成逐字加空格（此前仿真没建模这一格式,词级策略其实活着但窗口同样只有±5）②重锚点 40 字符窗只够 20 汉字追赶距离,读者念过头几秒即永久出窗。修复：锚点搜索窗 40→300（仿真:缺口50汉字由卡死33→追至249,含12%同音字噪声）。另加开关式匹配调试日志（defaults matchDebugLog,写 ~/Library/Logs/Textream-match.log,含识别错误码/重启/匹配三元组）,下一层机制若存在（识别流死亡/数字转写）日志直接现形。回归工具更新为 scripts/match-repro.swift（按真实加空格源建模,旧 ab 版已删）。教训:仿真必须照抄被仿对象的数据格式,差一个空格结论全歪。
